describe("Given the loginUser function", () => {
  describe("When it receives a request with a username and password belonging to the database", () => {
    test("Then it should response with status 200 and a token in the body", () => {
      // const req = {
      //   username: "papafrita",
      //   password: "papafrita"
      // }
    });
  });
});
